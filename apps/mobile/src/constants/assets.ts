/** Assets 3D locaux (côté client — non fournis par l'API). */
export const COFFEE_MODEL = require('@/src/public/models/coffee.glb');

/** Image de remplacement quand un article n'a pas d'image enregistrée en base. */
export const NO_IMAGE = require('@/assets/images/no-image.png');
